spring:
  application:
    name: api-gateway

server:
  port: 8080

# JWT Configuration
jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}